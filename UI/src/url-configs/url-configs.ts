import { scheduled } from "rxjs";

export const urls = {
    api: "https://localhost:7038/api/",
    vacancy: "Vacancies",
    department: "Department",
    candidate: "Candidates",
    schedule: "Schedule",
}